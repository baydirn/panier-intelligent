# Backend Environment Variables

# Mot de passe admin (changez-le!)
ADMIN_PASSWORD=VotreMotDePasseSecure2025

# JWT Secret (générez avec: openssl rand -base64 32)
JWT_SECRET=changez-moi-en-production-utilisez-openssl-rand-base64-32

# URL du frontend (pour CORS)
FRONTEND_URL=http://localhost:5174

# Port du serveur
PORT=3001

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=panier_intelligent
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_POOL_MAX=20

# Optional: External APIs (for future features)
# GOOGLE_MAPS_API_KEY=your_google_maps_key
# OPENAI_API_KEY=your_openai_key
